import curry from "Function/curry/curry"



/**
 * Invokes the interceptor function with a value, then simply returns that value.
 *
 * @param interceptor
 * @param value
 */
const tap = (interceptor, value) =>
	null



export default curry(tap)