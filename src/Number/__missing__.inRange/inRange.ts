/**
 * Returns a boolean indicating whether a given value is within a lower and upper bounds.
 *
 * @param [ upper, lower ]
 * @param value
 * @returns {number}
 */
const inRange = ([ upper, lower ], value) =>
	null



export default inRange