/**
 * Return a random number between the inclusive lower and upper bounds.
 *
 * @param [ upper, lower ]
 * @param seed
 * @returns {number}
 */
const random = ([ upper, lower ], seed) =>
	null



export default random